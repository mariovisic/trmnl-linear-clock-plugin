{% assign config = trmnl.plugin_settings.custom_fields_values %}
{% assign offset = trmnl.user.utc_offset %}
{% assign subtitle = trmnl.plugin_settings.instance_name %}

{% render "linear_clock", config: config, offset: offset, subtitle: subtitle %}

<style>
  .hour-number {
    font-size: 1.25em;
  }
</style>
